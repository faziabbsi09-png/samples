name: Flutter Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable' # Yeh khud sabse naya version uthayega

      - name: Get dependencies
        run: flutter pub get
        working-directory: material_3_demo

      - name: Build APK
        run: flutter build apk --release --no-sound-null-safety
        working-directory: material_3_demo

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: star-chat-apk
          path: material_3_demo/build/app/outputs/flutter-apk/app-release.apk
          
